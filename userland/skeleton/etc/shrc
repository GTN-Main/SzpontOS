# /etc/shrc - System-wide configuration for interactive shells (shrc / bashrc equivalent)
# (C) Copyright by Szpont Industries. All rights reserved.

# Sensible colored prompt: root@szpontos:/# or user@szpontos:~$
if [ "$EUID" = "0" ] || [ "$UID" = "0" ]; then
    export PS1='\e[1;31m\u\e[0m@\e[1;32m\h\e[0m:\e[1;34m\w\e[0m# '
else
    export PS1='\e[1;36m\u\e[0m@\e[1;32m\h\e[0m:\e[1;34m\w\e[0m$ '
fi

# Environment defaults
export EDITOR=nano
export PAGER=cat

# Convenient aliases
alias ll='ls -la'
alias l='ls -l'
alias la='ls -A'
alias cls='clear'
alias md='mkdir -p'
alias rd='rm -rf'
alias ..='cd ..'
alias ...='cd ../..'

# Built-in help command
help() {
    cat << 'EOF'
SzpontOS Shell (sh) - Commands & Guide:
  help            - Display this help message
  echo [args...]  - Print text to standard output
  pwd             - Print current working directory
  cd <path>       - Change current working directory
  exit [code]     - Exit the shell

Core Rescue / Boot Commands (/bin/):
  cat, chmod, chown, date, df, dmesg, hostname, ifconfig,
  insmod, kill, ls, lsmod, mkdir, modinfo, mount, ping,
  poweroff, ps, reboot, rm, rmmod, shutdown, sleep, su,
  sync, sysctl, touch, uname

User Applications & Utilities (/usr/bin/):
  clear, clock, curl, donut, env, fastfetch, file, find,
  free, git, grep, groupadd, head, hello, host, httpd, httpget,
  id, killall, lspci, lsusb, makaljer, nano, nc, openssl,
  startx, sudo, szpontdesktop, szpontlogin, szponterm, tail,
  top, uptime, useradd, userdel, wc, whoami, xterm, zsh

Keyboard Shortcuts & Line Editing:
  Up/Down Arrows  - Command history navigation
  Left/Right      - Move cursor left / right
  Home / End      - Jump to beginning / end of line
  Tab             - Auto-complete commands and file paths
  Ctrl+C          - Cancel current line
  Ctrl+D          - Exit shell on empty line / Delete char under cursor
  Ctrl+L          - Clear screen and redraw prompt
  Ctrl+U          - Clear entire input line
  Ctrl+K          - Kill from cursor to end of line
  Ctrl+A / Ctrl+E - Jump to beginning / end of line
  Delete          - Delete character under cursor
  Backspace       - Delete character before cursor
EOF
}
